# 2026-08-25

## 安装 skill：push_go_label

- 从微信收到的 zip 安装了 `push_go_label` 到用户级 `~/.kunlunxiaozhi/skills/push_go_label/`（SKILL.md + scripts/push.py）。
- 用途：把 Go 标注数据收集表（Excel）推送到 `https://go.jzxhnh.com/u/submit`。
- 安全审计：无 P0/P1 风险（无 eval/exec/subprocess、无危险文件操作、仅请求 go.jzxhnh.com）。密码明文存 `~/.codex/push_go_label/config.json`。
- 依赖：openpyxl + requests 已确认在默认 venv `/Users/fengyin/.workbuddy/binaries/python/envs/default/` 可用（requests 本次新装 2.34.2）。
- 用法：`push.py setup`（初始化账号密码，一次性）→ `push.py push --xlsx 表.xlsx`（校验 15 必填字段后逐条提交），支持 `--dry-run`。

## 配置完成

- 已用账号 `yuxiamei`（余霞梅）登录 go.jzxhnh.com 验证通过，并保存配置到 `~/.codex/push_go_label/config.json`。以后直接 `push.py push --xlsx 表.xlsx` 即可，无需再输入账号密码。

## 首次真实提交成功

- 提交了 `49-clipboard【10】/_shared/收集表_汇总.xlsx`，10/10 条全部成功，submission_id 12461–12470，状态均为「待初审」。
- 该表 10 条：diagnosis ×6（001–006）+ bugfix ×4（007–010），15 必填字段校验全通过。

## 第二次提交成功

- 提交了 `50-map-visualization【10】/_shared/收集表_汇总.xlsx`，10/10 条全部成功，submission_id 12472–12481，状态均「待初审」。
- 该表 10 条：diagnosis ×6（caption-*001–006）+ bugfix ×4（007–010）。
